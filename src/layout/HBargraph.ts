import { AbstractInputItem } from "./AbstractInputItem";

export class HBargraph extends AbstractInputItem {
    layout: TLayoutProp = {
        type: "hbargraph",
        width: 5,
        height: 1,
        sizing: "horizontal"
    };
}
